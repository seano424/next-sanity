/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'cdn.sanity.io'],
  },
  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig
